import React from 'react';
import logo from '../g-dino.png';

const Header = (props) => {
    
    
    return (
    
    <header>
        <img src={logo} alt="dino-logo"/>
    </header>
    
    )
}

export default Header;